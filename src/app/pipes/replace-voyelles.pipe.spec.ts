import { ReplaceVoyellesPipe } from './replace-voyelles.pipe';

describe('ReplaceVoyellesPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceVoyellesPipe();
    expect(pipe).toBeTruthy();
  });
});
